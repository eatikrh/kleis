// No template - using minimal defaults
#set page(margin: 1in)
#set text(size: 11pt)

#set document(
  title: "Deep Learning for Quantum State Tomography",
)

#align(center)[
  #text(size: 20pt, weight: "bold")[Deep Learning for Quantum State Tomography]
]

This thesis presents deep learning approaches for quantum state reconstruction.

#heading(level: 1)[Introduction] <ch:intro>

Quantum computing enables exponential speedups for specific problems.

$ i planck.reduce (diff Psi)/(diff t) = H Psi $ <schrodinger>

$ cal(L) = cal(L)_(r) + beta cal(L)_("KL") $ <loss>

#heading(level: 1)[Methods] <ch:methods>

#heading(level: 1)[Results] <ch:results>

$ F = 0.99 plus.minus 0.01 $ <accuracy>

#heading(level: 1)[Conclusion] <ch:conclusion>
