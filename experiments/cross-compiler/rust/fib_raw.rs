// Generated by Kleis Cross-Compiler (raw output)
// https://github.com/eatikrh/kleis
//
// This is what decl_to_rust(fib_declaration) produces with the current
// placeholder number_to_string that returns "0.0" for all numbers.
//
// The actual codegen output format:

fn fib(n: u64) -> u64 {
    if (n < 0.0) { n } else { (fib((n - 0.0)) + fib((n - 0.0))) }
}

// Note: This won't compile because:
// 1. number_to_string returns "0.0" (placeholder)
// 2. Comparisons between u64 and f64 don't work
//
// With a proper number_to_string implementation, this becomes:
//
// fn fib(n: u64) -> u64 {
//     if (n < 2) { n } else { (fib((n - 1)) + fib((n - 2))) }
// }
//
// Which compiles and runs correctly!

