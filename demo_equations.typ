#set document(
  title: "Equations with AST Demo",
  author: "Kleis Project",
)

#set page(paper: "us-letter", margin: 1in)
#set text(size: 11pt)

#align(center)[
  #text(size: 20pt, weight: "bold")[Equations with AST Demo]
]

#align(center)[Kleis Project]

#heading(level: 1)[Abstract]
This document demonstrates equations stored with EditorNode AST that can be re-edited in the Equation Editor.

#heading(level: 1)[Physics Equations]

$ E = m times c^(2) $ <eq:einstein>

The famous mass-energy equivalence equation.

$ x = frac(plus_minus(-b, sqrt(b^(2) - 4 times a)), 2 times a) $ <eq:quadratic>

The quadratic formula for solving ax² + bx + c = 0.
